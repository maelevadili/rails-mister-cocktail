<h1><%= @cocktail.name  %></h1>

<h2>Cocktail recipe</h2>

<ul>
<% @cocktail.doses.each do |dose| %>
  <li>
    <%= dose.ingredient.name.capitalize %>: <%= dose.description %>
    | <%= link_to 'Delete', dose_path(dose),
    method: :delete,
    data: { confirm: "Are you sure?" } %>
  </li>
<% end %>
</ul>

<%= link_to new_cocktail_dose_path(@cocktail) do %> Add an ingredient <% end %>

<hr>
<%= link_to cocktails_path do %> Back to cocktail list <% end %>
